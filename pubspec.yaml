name: subscription_manager
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  page_transition: ^2.0.2   #ページ転移用
  url_launcher: ^6.0.9 #ハイパーリンク
  google_fonts: ^2.1.0 #google font

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
